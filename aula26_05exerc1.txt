import 'package:flutter/material.dart';

void main() {
  runApp(MainApp());
}

class MainApp extends StatelessWidget {
  const MainApp({super.key});
  @override
  Widget build(BuildContext context) {
    var linguagens = ['HTML', 'CSS', 'JavaScript', 'Dart', 'Python'];
    return MaterialApp(
      home: Scaffold(
        body: Center(
          child: Column(
            children: [
              Text(linguagens[0]),
              Text(linguagens[1]),
              Text(linguagens[2]),
              Text(linguagens[3]),
              Text(linguagens[4]),
            ],
          ),
        ),
      ),
    );
  }
}
